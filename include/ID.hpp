#ifndef ID_HPP
#define ID_HPP

// IDs for the controls and the menu commands
enum ID {
    Bezier_MainWindow = wxID_HIGHEST,
    Bezier_Quit = wxID_EXIT,
    Bezier_About = wxID_ABOUT,
    Bezier_LogW = wxID_HIGHEST + 10
};
#endif